import Ember from 'ember';

/**
 * A simple grid for displaying fires.
 * @module incidents-grid
 * @example
 * {{incidents-grid incidents=model}}
 */

/**
 * @class IncidentsGrid
 * @extends Ember.Component
 */

export default Ember.Component.extend({
  classNames: ['incidents-grid']
});
